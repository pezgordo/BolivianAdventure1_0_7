//Creates numCircles radiuses and spaces them out evenly between minRadius and maxRadius

minRadius = 1;
maxRadius = (sprite_width / 2) * 0.8; 
//numCircles = 1;

//radiuses = array_create(numCircles);
radiuses = 0;
//alphaIntensity = 1; //number between 0(min) and 1(max)

//for(var i=0;i<numCircles;++i)
//{
  //  var d = maxRadius - minRadius;
    //var p = (i+1)/numCircles;
   //radiuses[i] = minRadius + d * p;
//}