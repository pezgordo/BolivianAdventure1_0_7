
	if (!file_exists("checkpoint.ini")) && (instance_exists(obj_cactus_dead))
	//if (oCactus.dead == 1)
	{
		
		
		ds_list_clear(global.lista_checkpoint)

	}
	


//if (global.vidas == -2)
//{
	
//	ad_timer = ad_timer -1;
	
//	if ad_timer == 0
//	{
//		slide_transition(TRANS_MODE.RESTART);
//	}
	
	
	//slide_transition(TRANS_MODE.RESTART);
	
	//if(alarm[0] == -1) alarm[0] = room_speed * 2;
	
	/*
	if(AdMob_Interstitial_IsLoaded())
			{
				AdMob_Interstitial_Show();	
			}
			
	ad_timer = ad_timer - 1;
	
	if (adtimer == 0)
	{
	
	slide_transition(TRANS_MODE.RESTART);
	adtimer = 60;
	}
	
	*/
	
//}

/*
var i

for (i = 0; i < lista_checkpoint; i+=1)

	{
	 show_debug_message(string(lista_checkpoint(i)))
	 
	}
	
